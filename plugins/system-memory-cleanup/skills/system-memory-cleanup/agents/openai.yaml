interface:
  display_name: "System Memory Cleanup"
  short_description: "Use the System Memory Cleanup workflow"
  default_prompt: "Use $system-memory-cleanup to help with this request."
policy:
  allow_implicit_invocation: true
