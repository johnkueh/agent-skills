interface:
  display_name: "System Disk Cleanup"
  short_description: "Use the System Disk Cleanup workflow"
  default_prompt: "Use $system-disk-cleanup to help with this request."
policy:
  allow_implicit_invocation: true
