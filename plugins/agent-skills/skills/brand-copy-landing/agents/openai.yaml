interface:
  display_name: "Brand Copy Landing"
  short_description: "Use the Brand Copy Landing workflow"
  default_prompt: "Use $brand-copy-landing to help with this request."
policy:
  allow_implicit_invocation: true
