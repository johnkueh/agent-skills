# Agent skills workspace

- Use `pnpm`, not npm or yarn.
- Edit canonical skills under `skills/`.
- Run `pnpm test` after any skill or packaging change.
- Never edit generated files under `plugins/` directly.
- Keep portable skill instructions independent of harness-specific tool names and config paths.
